apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: cloudscale
  labels:
    app: order-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3002
      protocol: TCP
      name: http
  selector:
    app: order-service
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: order-service
  namespace: cloudscale
  labels:
    app: order-service
