apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "analytics-chart.fullname" . }}
  labels:
    {{- include "analytics-chart.labels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "analytics-chart.fullname" . }}
  labels:
    {{- include "analytics-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "analytics-chart.selectorLabels" . | nindent 4 }}
