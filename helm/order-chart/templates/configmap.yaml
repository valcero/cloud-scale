apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "order-chart.fullname" . }}-config
  labels:
    {{- include "order-chart.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
